#include <stdio.h>
#include "main.h"

/**
 * main - entry
 * Return: lower alphabets
 */

int main(void)
{
	void print_alphabet(void)
	{
		char alphs;

		for (alphs = 'a'; alphs <= 'z'; alphs = alphs + 1)
		{
			putchar(alphs)'
		}
		putchar('\n');

		return(alphs);
	}
	print_alphabet();

	return (0);
}
